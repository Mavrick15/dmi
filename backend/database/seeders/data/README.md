# Données des seeders

Les fichiers JSON CIM-10 ont été déplacés dans **`database/data/`** pour éviter qu’ils soient chargés comme modules ESM par ts-node.

Voir **`database/data/README.md`** pour la documentation des données CIM-10.
